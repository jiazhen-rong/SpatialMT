% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/other_metrics.R
\name{moran_I_knn}
\alias{moran_I_knn}
\title{Global Moran's I}
\usage{
moran_I_knn(
  voi = NULL,
  N = NULL,
  af.dm = NULL,
  spatial_coords = NULL,
  coverage_thresh = 1,
  k = 50,
  seed = 42
)
}
\arguments{
\item{voi}{Character vector of variant IDs to analyze. The first element is
also used to generate the control.}

\item{N}{Numeric matrix of coverage counts (variants x spots).}

\item{af.dm}{Numeric matrix of allele frequencies (variants x spots).}

\item{spatial_coords}{Numeric matrix/data.frame of spatial coordinates
with rownames as spot IDs (rows = spots, cols = spatial dimensions).}

\item{coverage_thresh}{Minimum coverage required to include a spot.
Default is 1.}

\item{k}{Number of nearest neighbors used to build the kNN graph. Default is 50.}

\item{seed}{Integer seed for reproducible control permutation. Default is 42.}
}
\value{
A matrix with rows \code{c(voi, "control")} and columns:
\code{mi_list} (Moran's I statistic) and \code{p_list} (p-value).
}
\description{
Computes global Moran's I for each variant using allele frequency values
across spots, with spots filtered by a minimum coverage threshold.
Spatial neighbors are defined via a k-nearest neighbor (kNN) graph built
from \code{spatial_coords}, and weights are row-standardized (\code{style = "W"}).
}
\details{
In addition to the variants in \code{voi}, the function computes a
"control" Moran's I by permuting (\code{sample}) the allele frequencies of
the first variant in \code{voi} while keeping coverage and coordinates fixed.

Uses \code{spdep::knearneigh}, \code{spdep::knn2nb}, \code{spdep::nb2listw}
and \code{spdep::moran.test} with \code{zero.policy = TRUE}.
}
